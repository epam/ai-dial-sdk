Look at the OpenAI SDK types that correspond to the Event stream generated by the Responses API.

Put them all into the summary table:

| Event type | Main fields | Link |
| ... | foo, bar.quaz | [link](#....)

I want you to group them logically - e.g. open/delta/close events are close together.
Server-side, Client-size tool calls are group together too etc.

# Event this

Schema as yaml: ... # not literal, you can use references to massive child objects in the event schema

What it does.

# Event that

Schema as yaml: ...

----

Finally I want you to develop a draft of SDK language (in Python pseudo-code) for the server that exposes Responses API, e.g.:

```
with response.create_output_item() as item: # enter: <added-event-type>; exit: <done-event-type>
  with item.create_text_content_part() as part: # enter: <added-event-type>; exit: <done-event-type>
    part.append_delta("hello")
    part.append_delta(" world!")
```

So basically I want:

1. to have a single point of entry - a certain 'response' object of type "Response"
2. for the create/delta/done-kind of life-cycle use the context managers.
3. enable creation of specialize objects (like output item) that have their our useful API
4. enable fluid interface
5. also suppose that you have an access to "request" object that is typed as a "create a response" request (from openai Python SDK)

Sketch the API - I want to see for *each event type* a minimalistic piece of pseudo-code that generates this event type.
Annotate each line with the type of event that it produces.

Place all intermediate research files in the folder .research-responses-api-sdk (with .gitignore=*).

Let's write the summary table into event.md file and pseudo-code into api.md file.
