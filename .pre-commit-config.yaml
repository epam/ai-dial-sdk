repos:
  - repo: local
    hooks:
      - id: make-format
        name: Make Format
        entry: make format
        language: system
        types: [python]
        stages: [pre-push]

      - id: make-lint
        name: Make Lint
        entry: make lint
        language: system
        types: [python]
        stages: [pre-push]
